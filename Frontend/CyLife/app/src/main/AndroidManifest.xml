<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.cylife">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Set application settings -->
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.cylife"
        tools:targetApi="31">

        <!-- LoginActivity (launch activity) -->
        <activity
            android:name="com.example.cylife.WelcomeActivityClub"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".WelcomeActivity"></activity>
        <activity android:name=".SignupActivity"></activity>
        <activity android:name=".organizationActivity"></activity>
        <activity android:name="WelcomeActivityStudent"></activity>
        <activity android:name=".clubActivity"></activity>
<!--        <activity android:name=".WelcomeActivityClub"></activity>-->
        <activity android:name=".EditClub"></activity>
        <activity android:name=".EditUser"></activity>
        <activity android:name=".EventsActivity"></activity>
        <activity android:name=".ShowEvents"></activity>

    </application>
</manifest>